{
  "name": "se-pon-desktop",
  "version": "1.0.0",
  "description": "ポン出しアプリ デスクトップ版",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "npx electron-packager . PonDashiApp --platform=win32 --arch=x64 --out=dist_app --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "electron": "^29.0.0",
    "electron-builder": "^26.8.1"
  }
}
